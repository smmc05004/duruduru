# T6 — 관광 데이터·목적 적합성 고도화 결과 비교·검증 보고

> 기준일: 2026-09-10. 브랜치 `docs/tourism-recommendation-upgrade`. 제품 SSOT: [관광 데이터·목적 적합성 기획서](../product/TOURISM_RECOMMENDATION_UPGRADE.md). 작업 계획: [TOURISM_RECOMMENDATION_WORK_PLAN.md](TOURISM_RECOMMENDATION_WORK_PLAN.md).
>
> T1~~T5 구현(커밋 `3ba31e1`~~`cd43a9b`)의 변경 전/후를 비교하고, 통합·E2E·단위 검증을 정리한다. **결론: 수용 기준 1·2·3·4·6은 충족. 수용 기준 5의 "개선되지 않은/후퇴 사례"가 다수이고, `interest` 역할이 원거리 대도시로 쏠리는 구조적 현상은 D4 규칙에 충실한 결과이나 제품상 바람직한지 PM 판단이 필요하다.**

## 1. 검증 실행 결과

| 검증              | 명령                                                     | 결과                                                                                                                                      |
| ----------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 단위·타입·빌드    | `npm run verify`                                         | 통과                                                                                                                                      |
| 고도화 단위       | `npm run test:enhancement`                               | 18 스위트 · 147 통과                                                                                                                      |
| 전체 Jest         | `npx jest`                                               | 322 통과 / **19 실패** — 전부 `app/__tests__/page.test.tsx`(이 작업 전부터 깨진 PoC, `f95699d`에서도 동일 19 실패 확인). **신규 회귀 0.** |
| E2E               | `npm run test:e2e -- --repeat-each=2`                    | 통과(로컬 2회). 신규 `T6 중심 근거 카드…` 포함.                                                                                           |
| 통합(외부 호출 0) | `lib/__tests__/enhancement-t6-search-no-network.test.ts` | 통과                                                                                                                                      |

### 성능 회귀와 수정

T5의 새 분류로 프로필 관광지가 올바르게 늘면서(6,970 → 13,628 매칭 대상), `selectPlaces`가 매 호출마다 O(n²) `facilityGroups`를 큰 풀에 재실행해 검색이 느려졌다.

| 입력(Jest, 웜)         | 변경 전(`f95699d`) | T5 직후 | facilityGroups 메모 후 |
| ---------------------- | -----------------: | ------: | ---------------------: |
| 부산 · 자연+역사       |               1.2s |    3.5s |                   1.6s |
| 부산 · 역사+문화       |              ~1.2s |    6.7s |                   2.6s |
| 서울 · 역사 단일       |              ~0.9s |    1.0s |                   0.5s |
| 서울 · 5개 관심사 전부 |                  — |       — |                   3.8s |

수정: `facilityGroups`를 기본 보정 목록일 때만 `WeakMap`으로 풀 배열 참조 기준 메모이즈(`lib/mvp-phase-two-planner.ts`). 순수 함수라 결정성 유지 — 비교 매트릭스 48셀 결과가 **바이트 동일**함을 확인. 이 수정 전에는 부산 실검색 E2E가 5s 가시성 대기를 넘겨 실패했고, 수정 후 통과.

## 2. 변경 전/후 비교 (searchPhaseTwo, 정상본 JSON, 네트워크 0회)

- **before 기준선**: `git worktree add ../duruduru-before f95699d`(= origin/main). 옛 코드 + 옛 `data/region-profiles.json`(249지역 · schemaVersion 없음).
- **입력 매트릭스(48셀)**: 출발지 4개(서울 / 부산 / 강릉시·비광역시 / 수원 팔달구·비광역시) × 관심사 6세트(역사 / 자연 / 문화 / 레저 / 휴양 단일, 역사+문화) × 시간 2개(day `08:00→익일20:00`, night `18:00→익일20:00`).
- 재현: `lib/__tests__/enhancement-t6-comparison.test.ts`(`T6_OUT`로 JSON 덤프) → `scripts/report-tourism-recommendation-comparison.mjs`. 전체 표는 아래 6절.

### 2.1 요약 수치

| 지표                                 |         변경 전 |          변경 후 |
| ------------------------------------ | --------------: | ---------------: |
| 성공 셀                              |              48 |               48 |
| `interest` 후보가 그대로인 셀        |               — |           2 / 48 |
| `interest` 왕복 > 240분(편도 2h+) 셀 |          4 / 48 |      **34 / 48** |
| `interest` 왕복시간 min/중앙/max     | 20 / 62 / 360분 | 28 / 358 / 474분 |

`interest` 역할 후보의 변화(near = 왕복 ≤ 180분):

- **근거리 + 높은 목적 점수로 개선: 11셀.** 예: 부산·역사 → 창원(rt58, ps0.75), 부산·휴양 → 울산(rt80, ps0.76), 수원·휴양(night) → 서울(rt42, ps0.52), 수원·자연(night) → 안산(rt28, ps0.62).
- **근거리 후보가 원거리 후보로 대체: 29셀.** 예: 서울·역사(day) 수원(rt54) → 창원(rt434, ps0.75), 서울·자연(day) 안산(rt64) → 부산(rt474, ps0.83), 강릉·문화(day) 속초(rt80) → 부산(rt412, ps0.81), 수원·역사(day) 서울(rt42) → 창원(rt400).
- **동일/판단 보류: 8셀.**

### 2.2 `easy`·`relaxed`는 근거리를 계속 노출

`easy`(이동 부담 최소)와 `relaxed`(현지시간 최대)는 변경 전후 모두 근거리 시·군을 안정적으로 고른다. 사용자는 3장 중 근거리 선택지를 항상 본다. 바뀐 것은 **`interest` 카드가 거의 항상 "멀리 가는 여행"이 된 점**이다.

### 2.3 대표 명소 누락 복구 vs 실제 일정 선택 변화 (분리 평가)

- **대표 명소 복구(수용 기준 1): 성공.** 공산성(125949)·불국사(126166)·첨성대(126207)·미륵사지(127977) 모두 옛 프로필에 **부재** → 새 프로필에 `lclsSystm1=HS`·역사 판정으로 **포함**. 기존 필드(`cat*`) 결측으로 제외되지 않는다.
- **실제 일정 선택 변화(수용 기준 4): 별개.** 위 4곳 중 **첨성대**는 야간 출발 역사·역사+문화 셀에서 실제 초안에 배치된다(서울·부산·강릉·수원 출발 모두). 불국사·공산성·미륵사지는 이번 매트릭스 셀의 초안에는 뽑히지 않았다(hubRank·근접성 정렬에서 밀림). "프로필에 들어옴"과 "초안에 배치됨"은 다르며, 건수 증가를 곧바로 추천 품질 개선으로 해석하지 않는다.
- **높은 목적 점수 ≠ 유명 명소.** 서울·역사 → 창원(ps0.75)의 배치 명소는 진해루·성주사·삼밀사·장수암(비교적 덜 알려진 사찰)이다. hubRank(티맵 지역 연계 방문 중심성)는 명소 인지도·역사적 가치를 추적하지 않는다.

## 3. 전국·표본 품질

재현: `node scripts/report-tourism-evidence-quality.mjs [--md]`.

### 3.1 전국 집계 (baseYm 202608)

- 지역 249개 = 중심 `ok` 221 + 중심 `empty` 28.
- 프로필 관광지 총합 **13,628** · 중심 허브 총합 **20,705**(숙박·쇼핑 포함) · 확정 연결 `matched` **3,317**.
- 전체 연결률 **0.16**. `ok` 지역 연결률 분포 p10/p50/p90 = **0.07 / 0.15 / 0.27**.
- 낮은 연결률의 원인(T4 기록과 일치): 분모에 숙박 6,893·쇼핑 1,439 포함, 산·하천·대형 공원처럼 두 원천의 대표점이 500m를 넘는 경우, 프로필에 없는 상호(체인·전망대), 역·터미널 등 교통 허브. **보수적 게이트가 의도적으로 미연결을 많이 남긴다 — 기준을 느슨하게 바꾸지 않았다.**

### 3.2 표본 지역

| regionId      | 지역   | 프로필 관광지 | 중심 허브 | matched | 연결률 | 중심상태 |
| ------------- | ------ | ------------: | --------: | ------: | -----: | -------- |
| ktdb-zone-154 | 공주시 |            85 |       100 |      28 |   0.28 | ok       |
| ktdb-zone-171 | 익산시 |            83 |       100 |      20 |   0.20 | ok       |
| ktdb-zone-207 | 경주시 |           206 |       100 |      29 |   0.29 | ok       |
| ktdb-zone-97  | 과천시 |            27 |        63 |      11 |   0.17 | ok       |
| ktdb-zone-89  | 광명시 |            16 |        93 |      12 |   0.13 | ok       |
| ktdb-zone-108 | 파주시 |           142 |       100 |      30 |   0.30 | ok       |
| ktdb-zone-122 | 강릉시 |           167 |       100 |      31 |   0.31 | ok       |

특정 도시 순위를 통과 조건으로 고정하지 않았다. 공주·익산·경주가 무조건 상위에 오게 하는 이름/지역 가점은 없다(수용 기준 5).

### 3.3 `no-central-data` 28개 지역

광주광역시 5개 구 + 전라남도 22개 시·군 + 경기도 화성시. 프로필 관광지는 정상 보유(합 1,566). 중심 근거만 없어 **D4상 목적 점수 0**으로 처리되고, 카드에는 "지역 매력이 없다는 뜻이 아니에요" 안내가 붙는다. 원인은 원천(중심 관광지 API)의 202608 자료 공백이며 코드 문제가 아니다(T3 기록). 영향 범위: 이 28개 지역은 `interest` 역할 동점 경쟁에서 목적 점수 0으로 불리하지만, `easy`·`relaxed` 및 최종 선택·일정 생성은 정상 동작한다(비교 매트릭스에서 수원 출발 시 화성이 `easy` 후보로 정상 노출됨).

## 4. 통합 검증 (수용 기준 6)

`lib/__tests__/enhancement-t6-search-no-network.test.ts`:

1. **전역 `fetch`를 던지도록 교체**한 상태에서 매트릭스 입력(서울/부산/강릉/수원)으로 `searchPhaseTwo` 실행 → 전부 `success`, `fetch` 호출 0회.
2. 검색 진입 모듈 그래프(`mvp-phase-two-search`·`-planner`·`-regions`·`tourism-evidence`·`interest-classification`·`origin-regions`·`mvp-phase-two-types`·`mvp-region-data`·`app/api/search/route.ts`) 소스에 `@/lib/tour-api`·`axios`·`apis.data.go.kr`·`restaurants/route`·`detailIntro2` import과 `fetch(` 호출이 **없음**을 코드로 검증. 원천 클라이언트(`lib/tour-api.ts`, 음식점 route handler)는 선택 이후 경로에만 존재.

E2E `T6 중심 근거 카드…`도 `/api/search` 실호출 시 `phase-two/restaurants`·`attractions` mock 호출 수가 0임을 확인한 뒤에만 지역을 선택한다.

## 5. Playwright (핵심 흐름)

`npm run test:e2e -- --repeat-each=2` 전체 통과(로컬 2회). 주요 스펙:

- `home.spec.ts`: 검색은 음식점 조회 없이 후보 표시 / 역할 후보 선택 후에만 음식점 API 1회 / 선택 계획 관광지만 비차단 보강, 검색에서 상세 미호출 / 부산 출발 전송 / 표준(비광역시) 출발 선택→제출 / 당일 입력 거절 / 개인 일정 추가가 음식점 재조회 안 함 / 날짜 이동 / 숙소 메모 저장·복원.
- `enhancement-correction.spec.ts` R6: 서울·부산 실검색 → 식당 → 관광 날짜/시간/순서 편집 → 저장·복원, `details` 호출 수 = 관광 수, `meals` = 1.
- **신규 `T6 중심 근거 카드…`**: 서울·역사+문화 실검색 → `interest` 카드의 `방문 미리보기·추천 근거`를 펼쳐 "목적 근거 점수"·"중심 관광지 N곳" 노출 확인, 과장 문구(`전국 역사 1위`·`가장 인기 있는`) 없음 확인 → 그 카드 선택 → 초안 관광 블록 수 ≥ N(중심 근거 장소는 실제 초안의 부분집합) → 야간 휴식·점심·저녁·출발·복귀 블록 존재 → 저장·새로고침·복원.

실제 무료 API 확인은 제한 표본만(T0~T3 기록의 수동 조회). 대량 E2E 반복은 원천을 mock 한다.

## 6. 미해소 항목 · 제품 규칙 변경 제안 (임의 튜닝하지 않음)

### 6.1 `interest` 역할이 원거리로 쏠린다 (수용 기준 5 "개선되지 않은 경우")

**현상**: `interest` 후보 왕복시간 중앙값이 62분 → 358분. 34/48셀이 편도 2시간 이상. 야간 출발 셀에서는 특히 나쁘다(예: 서울 → 경주 왕복 396분·현지자유 257분, 부산 → 정선 왕복 430분·현지자유 240분).

**원인**: D4 정렬 키가 `충족 관심사 수 → 목적 근거 점수 → 세부 분류 다양성 → 실제 관광 수 → 왕복시간` 순이다. 목적 점수가 왕복시간보다 우선이라, 단일 관심사 검색처럼 다수 지역이 관심사를 충족하면 목적 점수가 사실상 결정한다. 목적 점수는 초안에 배치된 6곳이 각각 낮은 hubRank(1~~10)에 연결되면 최대이므로, **중심 관광지 자료가 조밀한 지역**(대도시, 또는 관광 밀집 시·군)이 구조적으로 유리하다. 광역시 그룹은 소속 구 전체의 중심 관광지를 모아 초안이 낮은 hubRank 6곳을 고르므로 목적 점수 0.75~~0.83에 이르는 반면, 단일 시·군은 대개 0.3~0.5다. D4가 경계한 "하위 구 순위 1위의 무제한 합산"이 원천 합산이 아니라 **초안 선정 경로로** 재현된다.

**완화 요소**: 근거리 후보는 `easy`·`relaxed`로 항상 노출되어 사용자가 3장 중 고를 수 있다. "관심사를 깊게 즐기는 여행" 카드가 원거리라는 해석도 가능하다.

**제품 규칙 변경 후보(택1 또는 조합, PM 결정 필요)**:

1. **왕복시간 밴드 안에서만 목적 점수로 정렬**: 최소 가용 왕복시간의 일정 배수(예: 1.5배) 또는 절대 상한 안의 후보만 목적 점수로 비교하고, 밴드를 넘으면 감점.
2. **가용 현지시간당 목적 점수 비율**: `목적 점수 / (현지 자유시간 시간)` 등으로 정규화해 원거리·저체류를 자연 억제.
3. **목적 점수를 거친 왕복시간 버킷 이후의 동점자로만 사용**(현재 5번째 키를 2번째로 이동한 것을 부분 되돌림).
4. **광역시 그룹의 목적 점수 정규화**: 구성 지역 수로 나누거나 구성 지역별 기여 상한.
5. **야간/저체류 방어**: `interest` 역할에 최소 현지 자유시간 하한(예: day 시나리오 대비 X% 이상)을 두고, 미달 시 근거리 후보로 대체.

### 6.2 연결률 16% · 미연결 3,317 vs 16,311

보수적 게이트(정규화 이름 동일 + 좌표 500m + 유일 후보)의 결과다. 숙박·쇼핑을 분모에서 빼도 큰 변화는 없다(대부분 `no-name-match`). 기준 완화는 잘못된 본체/부분 연결(공산성/공산성연지 등 금지 회귀 4쌍)을 되살릴 위험이 있어 하지 않았다. 연결률을 올리려면 (a) 검증된 수동 별칭 축적, (b) 좌표 반경 상향의 위험 평가, (c) 원천 확대 — 모두 별도 데이터 작업이며 이번 범위 밖.

### 6.3 계정 호출량 (값 미출력)

- TourAPI 프로필 재수집(T2): 26요청 · 실패 0 · 재시도 0(개발 일일 한도, 예산 900 이내).
- 중심 관광지 API(T3): 290요청(최초 262 + 재개 28) · 실패 0 · 재시도 0(예산 400 이내).
- 검색·E2E·비교 매트릭스: 관광 목록·중심·음식점·상세 **원천 호출 0회**(정상본 JSON만 읽음).

### 6.4 미구현 운영 자동화

- 관광 프로필 하루 1회, 중심 관광지 새 기준월 갱신은 **운영 목표**이며 예약 자동화(GitHub Actions 정기 실행 등)는 이번에 구성하지 않았다. 로컬 수집 → 검토 → 커밋 → 배포 순서만 존재한다.
- 중심 관광지 `empty` 28지역의 원천 자료 공백이 해소되면 재수집이 필요하다(과거 월 자동 혼합은 D2상 금지).

### 6.5 독립 reviewer 권장

D4 정렬 규칙 자체의 산출물이 제품 원칙("가용시간 우선", "실행 가능한 참고용")과 충돌할 여지가 있고, 성능 수정(`facilityGroups` 메모)과 선정 경로 변경이 함께 들어갔다. **구현 세션과 분리된 reviewer 세션의 독립 검토를 권장한다**(특히 6.1의 제품 규칙 판단은 PM, 코드 정합성은 reviewer).

### 6.6 리뷰 후속 수정 (2026-09-10, 같은 PR)

독립 리뷰에서 확인된 결함 3건을 같은 브랜치에서 수정했다. 제품 정책(가중치·역할 순서·목적 점수 공식)은 불변이며, T6 48셀 비교 매트릭스는 수정 전/후 **바이트 동일**이다.

- **결함 1·3 — 수집 완전성과 페이지별 재개(검증 통과 기준).** 공통 로직
  `scripts/lib/paged-collection.mjs`의 `collectPagedUnit`이 두 수집기(프로필=분류
  스펙, 중심=지역)를 처리한다.
  - **통신 성공 ≠ 검증 성공.** 페이지 항목 수가 기대치(마지막 전 페이지 =
    `pageSize`, 마지막 페이지 = `totalCount % pageSize`, 0이면 `pageSize`)에 못
    미치면(`totalCount=100`인데 1건 등) 그 페이지는 **불완전**으로 남기고
    체크포인트에 성공(`complete: true`)으로 저장하지 않는다.
  - **재개.** `--resume`은 `complete === true` 페이지만 건너뛴다. 불완전·미저장
    페이지는 다시 조회한다.
  - **일관성 붕괴 → 단위 재시작.** 페이지별 `totalCount` 변동 또는 페이지 간
    중복 ID는 수집 도중 스냅샷이 바뀐 징후라 부분 페이지를 신뢰할 수 없다. 그
    스펙/지역의 저장된 성공 페이지를 전부 무효화하고 1페이지부터 재수집한다(최대
    2회). 다른 정상 스펙/지역은 유지한다.
  - **완료 판정과 교체 차단.** 스펙/지역이 "완료"이려면 기대 페이지 전부 검증
    통과 + 페이지 간 중복 0 + `totalCount` 불변. 하나라도 어긋나면 미완료로 남기고
    정상본 교체에서 제외한다(중심: 지역 `status="failed"` + `validateDocument`;
    프로필: `document.completeness.blockers` + `validateDocument`). 임시 산출물
    `*.next.json`은 남긴다.
- **결함 2 — 새 세부 분류 적용.** planner의 `detailCategory`가 구 `cat3`/`cat2`만 봐서 새 분류만 있는 서로 다른 역사 명소(공산성 `HS010200` vs 불국사 `HS030100`)를 세부 분류 다양성 지표가 구별하지 못했다. 공통 헬퍼 `detailClassification`(`lib/interest-classification.ts`)로 `lclsSystm3 → lclsSystm2 → cat3 → cat2 → 관심사 조합` 순으로 값을 정한다. 정렬 키·가중치·역할 순서는 불변.
- **회귀 테스트.** 불완전 응답(1/N건) → `--resume`에서 정상 응답으로 **실제 복구**되고 1·2페이지는 재호출하지 않음, 일관성 붕괴 → 1페이지부터 재수집, 검증 통과 페이지만 건너뜀 — 프로필·중심·공통(`collectPagedUnit`) 세 레벨에서.
- **검증.** `npm run verify` 통과. `npm run test:enhancement` 150 통과. `npx jest scripts/__tests__/` 109 통과. `npx jest` 367 통과 / 19 실패(전부 기존 PoC `page.test.tsx`, 신규 회귀 0). `npm run test:e2e -- --repeat-each=2` 24 통과. T6 48셀 비교 매트릭스 바이트 동일.

### 6.7 3라운드 리뷰 후속 수정 (2026-09-10, 같은 PR)

수집 재개 기능의 결함 2건을 같은 브랜치에서 추가로 고쳤다. **추천 정책·가중치·역할 순서·목적 점수 공식은 불변이고, 변경 범위는 전부 `scripts/`(수집기)라 `lib/` 수정이 없다 → T6 48셀 비교 매트릭스는 구성상 영향 없음.**

- **결함 1 — 페이지 검증에 필수 식별자 유효성 포함.** 기존 페이지 완료 판정이 응답 배열 **길이만** 봐서, `totalCount=2`에 `[{contentId:"a"}, {}]`처럼 식별자가 빈 항목이 섞여도 `complete`로 저장됐다. 그 페이지가 체크포인트 `pages`에 남아 `--resume` 해도 재조회 없이 최종 `validateDocument` 실패가 무한 반복됐다. `scripts/lib/paged-collection.mjs`에 공유 함수 `hasRequiredId`/`pageItemsAllHaveId`를 두고, 페이지 완료 판정을 `isPageValidated`(항목 수 + 모든 항목의 필수 식별자)로 바꿨다. 필수 식별자는 프로필 = `contentId`, 중심 = `hubTatsCd`이며 두 수집기가 이미 넘기는 `idOf`를 그대로 쓴다(중복 로직 없음). 식별자 누락 페이지는 체크포인트에서 빠지고 스펙/지역 `done`도 false다. 오래된 체크포인트에 `complete`로 저장돼 있어도 seed 단계에서 배제해 재조회한다. `validateDocument`도 같은 함수로 항목 식별자 결측을 교체 차단 사유에 추가한다.
- **결함 2 — 검증된 페이지마다 체크포인트 영속 저장.** 체크포인트가 스펙/지역 종료·예외 시점에만 저장돼, 페이지 2 검증 후 프로세스가 죽으면 디스크에 아무것도 안 남았다. `collectPagedUnit`에 `onProgress` 훅을 두어 **정상 페이지 검증 직후**와 **일관성 붕괴로 단위를 재시작할 때(폐기 목록 `{}`)** 즉시 호출하고, 두 수집기 CLI가 이 훅에서 실제 체크포인트 파일(`data/region-profiles.checkpoint.json`, `data/central-attractions.checkpoint.json`)에 flush 한다. 뒤에 더 받을 페이지가 있을 때만 저장해 단일 페이지 수집(중심 API의 일반적 경우)의 쓰기 폭증을 피한다. 재시작 시 폐기는 재수집을 시작하기 **전에** 디스크에 반영된다.
- **회귀 테스트.** 공유 함수 3종, ID 누락 페이지 미저장·`--resume` 복구·오래된 체크포인트 배제, `onProgress` 페이지별·재시작 호출 — 공통(`collectPagedUnit`)·프로필·중심 세 레벨. 추가로 `scripts/__tests__/collector-cli-resume.test.js`가 `child_process`로 두 CLI를 실제 실행한다(통제된 fetch를 `DURUDURU_TEST_FETCH_MODULE`로 주입, 프로덕션은 전역 `fetch` 그대로): 하드 크래시 후 3페이지부터 재개·1·2 미호출·정상본 교체·실패 중 정상본 보존, 재시작 폐기 상태의 즉시 영속, 프로필 ID 누락 페이지 복구.
- **검증.** `npm run verify` 통과. `npm run test:enhancement` 150 통과. `npx jest scripts/__tests__/` 123 통과. `npx jest` 381 통과 / 19 실패(전부 기존 PoC `page.test.tsx`, 신규 회귀 0). `npm run test:e2e -- --repeat-each=2` 24 통과.

## 부록: 재현 스크립트

| 목적                       | 명령                                                                                         |
| -------------------------- | -------------------------------------------------------------------------------------------- |
| 비교 매트릭스 덤프(after)  | `T6_OUT=/tmp/after.json npx jest enhancement-t6-comparison`                                  |
| 비교 매트릭스 덤프(before) | before 워크트리에서 동일 스펙 실행 → `/tmp/before.json`                                      |
| 비교 표 생성               | `node scripts/report-tourism-recommendation-comparison.mjs /tmp/before.json /tmp/after.json` |
| 품질 표                    | `node scripts/report-tourism-evidence-quality.mjs [--md]`                                    |
| 외부 호출 0 검증           | `npx jest enhancement-t6-search-no-network`                                                  |
