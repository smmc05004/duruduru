# DURUDURU 지속 구현 작업 계획서

> 상태 기준일: 2026-09-02 / 최신 병합: PR #22, `c5afef0` (`feat/data-quality-boundary`)
>
> 이 문서는 다음 employee 작업을 재개하는 **실행 현황의 단일 진입점**이다. 제품 요구사항과 결정의 단일 출처는 각각 `docs/product/PRD.md`, `docs/product/DECISIONS.md`, `docs/product/FUNCTIONAL_SPEC.md`이며, 이 문서는 그것을 바꾸지 않는다.

## 1. 사용하는 방법과 상태 규칙

작업을 시작할 때는 이 문서에서 `다음`인 에픽 한 개를 선택하고, 연결된 기능 ID·선행 조건·수용 기준을 읽는다. 상태의 뜻은 다음과 같다.

| 상태             | 뜻                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------- |
| 완료             | 기능 명세의 현재 수용 기준을 실제 데이터 계약으로 충족했고, 코드·테스트 근거가 있다.                 |
| 부분 완료 (목업) | 화면 또는 규칙은 동작하지만 PoC/임시 데이터 또는 미확정 정책에 의존한다. **정식 MVP 완료가 아니다.** |
| 부분 완료 (조사) | 조사 결과나 작업 준비는 남았지만 구현 완료 조건을 만족하지 않는다.                                   |
| 다음             | 현재 선행 조건으로 바로 시작할 employee 작업이다.                                                    |
| 대기/차단        | 제품 결정 또는 앞 에픽 산출물이 없어 구현을 시작할 수 없다.                                          |
| 미착수           | 관련 구현·조사 근거가 없다.                                                                          |

`완료`와 `부분 완료 (목업)`을 혼동하지 않는다. 특히 `lib/mock-data.ts`의 값, PoC 화면, 고정 이동시간은 제품 정책이나 정식 데이터가 아니다.

## 2. 현재 기능 현황

| 기능 ID                        | 우선순위 | 상태             | 현재 근거와 남은 완료 조건                                                                                                                                                                                                                                                                                                                              |
| ------------------------------ | -------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-01 여행 조건 입력·검증       | P0       | 완료             | E3가 `lib/support-conditions.ts`의 버전 `2026-09-02` 지원 조건(서울특별시·대전광역시·부산광역시의 도시 ID·대표 기준점, 경주·공주·강릉 시 단위, 자차, KST)을 입력·검증·제출 요약에 연결했다. `lib/trip-conditions.ts`는 원문, 출발지 기준점 스냅샷, 지원 조건 버전, KST 여행 유형을 보존하며 Jest·Playwright가 목록 밖 입력 차단과 핵심 흐름을 검증한다. |
| F-02 목적지 가능 여부·추천     | P0       | 부분 완료        | E4의 `lib/recommendation.ts`, `lib/recommendation-request.ts`, `lib/recommendation-data-adapter.ts`, `lib/__tests__/recommendation-request.test.ts`가 E3 유효 조건, 왕복 이동·최소 체류 판정, 결정론적 통과 후보 정렬, 시간 제약 결과 없음과 데이터 결측 결과를 계약 기반으로 구분한다. PoC 목업은 기본 추천 경로에서 제거했다.                         |
| F-03 점수·추천 근거            | P0       | 부분 완료        | E4가 `lib/trip-policy.ts`의 결정론적 점수·동점·정책 버전과 태그/이동시간 provenance를 결과 모델에 보존한다. 실제 이동시간·태그 데이터가 아직 없으므로 기본 경로의 점수화 후보는 없고, F-05 화면 표시도 남았다.                                                                                                                                          |
| F-04 제약 기반 일정 생성       | P0       | 미착수           | 현 `lib/planner.ts`와 `lib/mock-data.ts`의 일정 코드는 종료된 PoC다. 운영·휴무·장소 간 이동·식사·복귀 제약을 모두 만족하는 MVP 엔진 근거가 없다.                                                                                                                                                                                                        |
| F-05 근거·데이터 신뢰도 표시   | P0       | 부분 완료 (목업) | 추천 결과에 이동시간이 임시 추정값임을 표시한다(`lib/travel-time.ts`, `components/CandidateCard.tsx`). 장소·운영·휴무·체류·fallback·오래됨 상태를 결과까지 전달하는 데이터 계약과 UI는 없다.                                                                                                                                                            |
| F-06 축제 반영                 | P1       | 미착수           | PoC 축제 목업은 정식 구현 근거가 아니다. 실제 기간·지역·관심사 판정과 데이터 계약이 없다.                                                                                                                                                                                                                                                               |
| F-07 대표음식 기반 음식점 배치 | P0 / P1  | 부분 완료 (조사) | 음식 데이터의 소스·필드·후속 절차는 `docs/product/FOOD_DATA_NEXT_STEPS.md`에 조사돼 있다. 대표음식 목록 승인, 운영시간 파싱, 음식점 데이터 계약·배치 엔진은 없다. P1 동선 최적화·복수 후보·미식 큐레이션도 미착수다.                                                                                                                                    |
| F-08 일정 재생성·장소 교체     | P1       | 대기/차단        | 일정 수정 범위가 `보류`다(`docs/product/DECISIONS.md`, `docs/product/FUNCTIONAL_SPEC.md`). 결정 전 구현하지 않는다.                                                                                                                                                                                                                                     |
| F-09 대중교통·동행·날씨·예산   | P2       | 미착수           | P2이며 핵심 MVP 가설 검증 이후 별도 단계다. 대중교통 PoC UI는 정식 지원 근거가 아니다.                                                                                                                                                                                                                                                                  |

## 3. 에픽 현황과 실행 순서

### E1 시작 전 품질 기반 보강

E1을 시작하기 전에 Prettier, staged 파일용 Husky/lint-staged, 포맷 검사 게이트, Node 22.22.1 기준, CI의 Chromium E2E·실패 산출물·timeout·동시 실행 취소를 보강한다. 이 변경은 제품 기능이나 데이터 계약을 완료하지 않으므로 E1의 상태와 다음 작업은 그대로 **다음**이다. 완료 근거는 `package.json`, `.nvmrc`, `prettier.config.mjs`, `.prettierignore`, `.husky/pre-commit`, `.github/workflows/ci.yml` 및 해당 작업의 검증 결과로 남긴다. 2026-09-01 기준 `npm run format:check`, staged 파일의 Husky 훅, `npm run test:e2e`, `npm run verify`(하네스·포맷·lint·타입 검사·Jest 52건·프로덕션 빌드)는 모두 통과했다.

| 순서 | 에픽                          | 상태                      | 완료/부분 완료 근거                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 선행 조건·차단 사유                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---- | ----------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0    | E0 범위 고정                  | 계속 관리                 | 결정 매핑과 보류 목록이 `docs/product/FUNCTIONAL_SPEC.md` 9장 및 `docs/product/DECISIONS.md`에 있다.                                                                                                                                                                                                                                                                                                                                                                                  | 값이 필요한 보류·미기록 판단은 구현으로 우회하지 않고 PM/사용자 결정으로 갱신한다.                                                                                                                                                                                                                                                                                                                                                                |
| 1    | E1 도메인 계약·목업 분리      | 완료                      | `8dd0b41`, `009e450`, `lib/domain-data.ts`, `lib/poc-data-adapter.ts`, `lib/__tests__/poc-data-adapter.test.ts`가 공통 provenance/status와 9개 도메인 계약, 출발지 기준점, 반복 휴무·날짜 예외, 명시적 PoC 변환을 제공한다. 추천·이동시간·종료된 PoC 일정 투영은 계약 어댑터만 읽으며, `npm run verify`(Jest 56건·빌드 포함)와 Playwright를 통과했다.                                                                                                                                 | 실제 공공데이터·DB·지원 지역/수단 값·운영시간 파싱·일정 알고리즘은 포함하지 않았다. PoC 값은 `estimate` 상태와 출처/수집일로 남아 있으며 정식 데이터나 정책이 아니다.                                                                                                                                                                                                                                                                             |
| 2    | E2 데이터 품질 조사·수집 경계 | 부분 완료 (조사)          | PR #22 (`7c85756`, 병합 `c5afef0`)가 `lib/data-quality-survey.ts`, `scripts/fixtures/e2-data-quality.json`, `scripts/probe-data-quality.mjs`, `scripts/probe-tourapi.mjs`로 계약 기반 fixture 조사·필드 충족/결측 보고·첫 페이지/상세 표본 한계 표시를 추가했다. `npm run check:data-quality`와 `npm run verify`는 실행됐고, 독립 리뷰를 통과했다.                                                                                                                                    | **실제 TourAPI 서비스키가 없어 `npm run check:tour-api`는 실행하지 못했다.** 따라서 fixture의 모집단·충족률은 실측 결과가 아니며, 실제 관광·축제·음식점 응답 검증, 강릉 잔여 수집, 지역N문화 CSV 컬럼·행수 확인, 대표음식 교차 대조·사용자 승인에는 쓸 수 없다. 이들은 정식 데이터 전환 전의 후속 조사다.                                                                                                                                         |
| 3    | E3 조건 입력                  | 완료                      | `lib/support-conditions.ts`, `lib/trip-conditions.ts`, `app/page.tsx`와 Jest·Playwright가 승인된 지원 조건 v1, 자차 전용 UI, 목록 검증, KST 여행 유형 및 제출 조건 재현을 구현한다. PoC 출발지 목록 직접 의존은 제거했다.                                                                                                                                                                                                                                                             | 실제 TourAPI 응답, 출발지↔목적지 이동시간, 목적지 태그는 E3 범위 밖이며 E4의 실제 추천 후보 활성화 전 데이터 준비로 남는다.                                                                                                                                                                                                                                                                                                                       |
| 4    | E4 추천 엔진                  | 완료                      | PR #25의 `79dc282`, `e86ee6b`가 `lib/recommendation-data-adapter.ts`, `lib/recommendation-request.ts`, `lib/recommendation.ts`, `lib/travel-time.ts`, `lib/__tests__/recommendation-request.test.ts`로 E3 유효 조건과 E1 계약을 연결했다. 정책·KST·지원 조건 버전, 태그/이동시간 provenance, 계산값·동점 기록을 보존하며, 실측 이동시간 결측은 `data-unavailable`로, 시간 제약 탈락은 `no-results`로 구분한다. `npm run verify`(Jest 70건·빌드), Playwright와 독립 재리뷰를 통과했다. | 실제 TourAPI 이동시간·목적지 태그가 아직 없어 기본 경로에는 통과 후보가 없다. 허위 후보를 만들지 않는 의도된 결측 상태이며, 상세 신뢰도 표시는 E5에서 보강한다.                                                                                                                                                                                                                                                                                   |
| 5    | E5 추천 UI·신뢰도             | 완료                      | PR #26(병합 `1fbb717`, `f944786`, `5642a59`)이 E4의 `recommendations`·`no-results`·`data-unavailable`을 화면에 연결하고, 후보마다 목적지·태그·이동시간 출처/수집 시각/상태와 시간·관심사 근거를 표시한다. normal·estimate·fallback·stale·missing은 아이콘, 토큰 색, 실선/점선으로 구분한다. 데이터 부족은 실제 이동시간 결측 후보와 그 사유만 보이며 시간 조정 안내와 분리된다. Jest 76건, Playwright 6건, 독립 리뷰와 CI를 통과했다.                                                 | 실제 TourAPI 이동시간·목적지 태그가 없어 기본 경로에는 통과 후보가 없다. 허위 후보를 만들지 않는 의도된 결측 상태다.                                                                                                                                                                                                                                                                                                                              |
| 6    | E6 일정 엔진                  | **대기/데이터 수집 차단** | `feat/itinerary-generation`이 PRD·결정·데이터 계약·종료된 PoC를 대조했다. 2026-09-03 사용자 승인으로 숙소 복귀는 0분 시각 표시, 다음날은 목적지 기준점 시작으로 확정했고 야간 정합성은 E6 결과 관측 뒤 재검토한다. 같은 날 이동시간은 실시간 API가 아닌 정적 사전 수집으로 확정됐다. `recommendationDataAdapter`는 아직 실제 장소·음식점·정적 이동 데이터를 제공하지 않으며, 종료된 PoC의 운영시간·체류·고정 30분은 정식 정책으로 쓸 수 없다.                                         | 초기 3개 지역의 실제 장소 운영·휴무·체류시간, 출발지×목적지 3×3 일반 편도 이동시간표, 필요한 장소↔장소·장소↔음식점 도로 경로 거리·일반 이동시간, 대표음식과 음식점 운영·휴무·좌표를 출처·기준일·버전·상태와 함께 내부 계약으로 수집·정규화해야 한다. 출처/제공자 선택과 시간 산출·반올림 규칙도 이 데이터 준비에서 정한다. 실제 배치 가능한 음식점 후보가 0곳일 때만 식사 블록 예외를 적용한다. 이 데이터가 준비되기 전에는 E6을 구현하지 않는다. |
| 7    | E7 일정 UI·근거               | 대기/차단                 | 없음.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | E5·E6 완료가 선행이다.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8    | E8 P1 확장                    | 대기/차단                 | 없음.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | E6 이후에만 축제·F-07 P1을 검토한다. F-08은 일정 수정 범위 결정이 선행이다.                                                                                                                                                                                                                                                                                                                                                                       |
| 9    | E9 관측·품질 게이트           | 미착수                    | 현재 Jest·Playwright·`npm run verify`는 개발 품질 기반이며 제품 이벤트·데이터 품질 모니터링은 없다.                                                                                                                                                                                                                                                                                                                                                                                   | E3~E7의 실제 결과 모델과 데이터 계약이 선행이다.                                                                                                                                                                                                                                                                                                                                                                                                  |

## 4. 다음 employee 작업: E6 일정 엔진 (대기/데이터 수집 차단)

**현재 상태:** E5는 PR #26으로 병합됐다. E4의 계약 기반 추천 엔진은 실제 이동시간·태그가 없는 기본 경로에서 후보를 지어내지 않고 데이터 부족으로 종료하며, E5는 그 결측과 시간 제약 결과를 다른 화면 상태로 보인다.

**E6 시작 조건:** E5 병합과 2026-09-03 제품 결정은 완료됐다. 숙소 시설 데이터는 비범위이며 숙소 복귀는 0분 시각 표시, 다음날은 목적지 기준점에서 시작한다. 이동시간은 런타임 API·실시간 교통 대신 정적 사전 수집으로 제공한다. 이제 초기 3개 지역의 실제 장소 운영·휴무·체류시간, 출발지×목적지 3×3 일반 편도 이동시간표, 필요한 장소↔장소·장소↔음식점 도로 경로 거리·일반 이동시간, 대표음식·음식점 운영·휴무·좌표를 출처·기준일·버전·상태와 함께 내부 계약으로 준비해야 한다. 출처/제공자 선택, 시간 산출·반올림 규칙과 실제 데이터셋 수집은 아직 남았다. 데이터 결측을 임의값이나 실제 후보 0곳으로 바꾸지 않으며, 실제 음식점 후보가 0곳일 때만 식사 블록 예외를 적용한다.

**최소 범위:**

- 다음 employee 작업은 E6 엔진 구현이 아니라 초기 3개 지역의 E6 입력 데이터 수집·정규화 준비다. 여기에는 3×3 출발지→목적지 일반 편도 이동시간표와 필요한 도로 경로 쌍의 정적 데이터 준비 기준도 포함한다.
- E6의 구현은 위 데이터 준비가 검증된 뒤 별도 브랜치와 실패 테스트로 시작한다.

**E6 범위 밖(현재):** 실제 데이터 준비 전 E6 엔진 구현, 런타임 경로 API·실시간 교통 연동, PM 확정값 밖의 임의 fallback, PoC 고정 30분 재사용, 숙소 위치 데이터 도입.

## 5. E1 이후의 고정 순서

1. E2의 실제 프로브와 음식 데이터 후속 조사는 정식 데이터 전환 전까지 병행 가능한 데이터 준비로 유지한다.
2. E6은 `DECISIONS.md` 8절의 확정 정책과 초기 3개 지역 E6 입력 데이터가 기록된 뒤에만 시작하며, E7은 E6 완료 뒤에만 시작한다.
3. E8·E9는 P0 흐름과 그 결과 모델이 안정된 뒤에 진행한다.

이 순서는 제품 우선순위가 아니라 의존성 순서다. 새 제품 결정이 이 순서를 바꾸면 먼저 `docs/product/DECISIONS.md`와 이 문서의 상태·차단 사유를 함께 갱신한다.

## 6. 매 작업 후 갱신 규칙

employee는 작업 완료 보고·커밋·PR 준비 전에 다음을 수행한다.

1. 관련 기능 ID와 에픽의 상태를 실제 수용 기준으로 다시 판정한다. 목업 또는 미확정 정책 의존이 남으면 `완료`로 올리지 않는다.
2. 이 문서에 커밋 해시, 코드 경로, 실행한 Jest/Playwright/`npm run verify` 결과를 완료 근거로 남긴다. 실행하지 못한 검증은 이유와 위험을 함께 남긴다.
3. 완료 조건이 남으면 구체 항목을 `선행 조건·차단 사유`에 유지한다. 제품 판단이 필요하면 값을 정하지 말고 PM이 `DECISIONS.md`에 기록하도록 요청한다.
4. 하나의 에픽이 완료되면 상태를 바꾸고, 의존성상 가장 앞선 미완료 에픽 하나만 `다음`으로 표시한다.
5. 문서 변경만이면 위험에 비례한 검증을 실행한다. 기능·화면 구현이면 `docs/agent/WORK_CYCLE.md`의 전체 사이클과 독립 리뷰를 반드시 적용한다.
